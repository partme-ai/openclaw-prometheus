apiVersion: 1

datasources:
  - name: OpenClaw Prometheus
    uid: DS_PROMETHEUS
    type: prometheus
    access: proxy
    url: http://127.0.0.1:9090
    isDefault: true
    editable: true

  - name: OpenClaw Loki
    uid: DS_LOKI
    type: loki
    access: proxy
    url: http://127.0.0.1:3100
    editable: true
